// Expansion-era constants, mirroring backend internal/era. Project Quarm's
// Planes of Power era: level cap 65.

export const POP_MAX_LEVEL = 65

// maxLevel is the server level cap.
export function maxLevel(): number {
  return POP_MAX_LEVEL
}
